import { createIdentificationSchema } from "../shared/identification-factory";
import { enTranslations } from "./translations";

export const identificationSchema = createIdentificationSchema(enTranslations);
